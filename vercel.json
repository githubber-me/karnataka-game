{
  "installCommand": "npm --prefix app ci",
  "buildCommand": "npm --prefix app run build:vercel && rm -rf .vercel/output && mkdir -p .vercel && cp -R app/.vercel/output .vercel/output",
  "framework": null
}
